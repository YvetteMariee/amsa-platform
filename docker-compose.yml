version: "3.9"

services:
  db:
    image: postgres:15
    container_name: amsa_postgres
    environment:
      POSTGRES_USER: amsa
      POSTGRES_PASSWORD: amsa
      POSTGRES_DB: amsa_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: